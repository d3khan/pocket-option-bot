pocket_option:
  ssid: "${PO_SSID}"        # Will be interpolated from env
  is_demo: true

auth:
  username: "d3khan04"
  password_hash: "$2b$12$ScWRL3mEa0JSt1okBhvcq.itEDfmyaCFQ4fcIWYJ9QVY6z60rD96a"

jwt:
  secret_key: "${JWT_SECRET}"
  algorithm: "HS256"
  access_token_expire_minutes: 1440   # 24 hours

bot:
  base_stake: 1.0
  multiplier: 2.5
  max_stake: 16.0
  min_payout: 92        # percentage
  trade_duration: 30    # seconds
  max_consecutive_losses: 5
  max_daily_loss: 50.0  # USD

web:
  port: 8000
  session_secret: "${SESSION_SECRET}"

db:
  path: "/app/data/bot_data.db"